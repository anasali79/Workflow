{
  "name": "@workflow/functions",
  "version": "0.1.0",
  "private": true,
  "description": "Nhost Functions for Workflow Agent Platform — Action handlers, webhook, scheduled, and db-event triggers.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node --watch dist/trigger-workflow-run/index.js"
  },
  "dependencies": {
    "pg": "^8.14.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pg": "^8.11.11",
    "typescript": "^5"
  }
}
